# 📖 对照翻译：Installation

> Source: `docusaurus/docs/cms/installation.md`  
> Upstream SHA: `b912b580362b42ad783e7d811c7e9de1acf68534`

**Original:** Installation

**中文译文:** 安装

**Original:** Strapi projects can be installed locally via CLI or run in Docker containers for development and production.

**中文译文:** Strapi 项目既可以通过 CLI 安装到本地，也可以在 Docker containers 中运行，用于 development 和 production 环境。

**Original:** Strapi projects can be installed either locally on a computer or on a remote server. The following installation guide provides step-by-step instructions on how to install and create a new Strapi project on your local machine:

**中文译文:** Strapi 项目既可以安装在本地计算机上，也可以部署到远程服务器。以下安装指南将逐步介绍如何在本地机器上安装并创建新的 Strapi 项目：

**Original:** CLI — Create a project on your local machine using the CLI.

**中文译文:** CLI — 使用 CLI 在本地机器上创建项目。参见 [CLI 安装指南](/cms/installation/cli)。

**Original:** If you already have an existing Strapi project on your local machine, the following guide covers running it in Docker containers for development and production:

**中文译文:** 如果本地已经存在 Strapi 项目，可以参考以下指南，将项目放入 Docker containers 中运行，分别用于 development 和 production：

**Original:** Docker — Build and run a Strapi project in Docker for development and production.

**中文译文:** Docker — 使用 Docker 构建并运行 Strapi 项目，用于 development 和 production。参见 [Docker 指南](/cms/installation/docker)。
