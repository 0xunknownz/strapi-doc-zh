# 📖 对照翻译：Project structure

> Source: `docusaurus/docs/cms/project-structure.md`  
> Upstream SHA: `b6ffbd796cfd126cde4e13718f540dac4aae1d34`

**Original:** Project structure

**中文译文:** 项目结构

**Original:** A Strapi project's structure varies depending on whether it was created with TypeScript (the default with `--quickstart`) or vanilla JavaScript, and is displayed through an interactive project structure component.

**中文译文:** Strapi 项目的目录结构会根据项目使用 TypeScript（使用 `--quickstart` 时的默认选项）还是原生 JavaScript 而有所不同。官方文档通过交互式 project structure 组件展示具体结构。

**Original:** The structure of a Strapi project depends on whether the project was created with TypeScript (which is the default if you used the `--quickstart` option while creating the project) or with vanilla JavaScript, and looks like the following:

**中文译文:** Strapi 项目的结构取决于创建项目时选择的是 [TypeScript](/cms/typescript) 还是原生 JavaScript。使用 `--quickstart` 创建项目时，TypeScript 是默认选项。具体目录结构请参阅官方交互式 project structure 页面。
