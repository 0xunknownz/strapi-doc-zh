# 📖 对照翻译：qs query introduction

> Source: `docusaurus/docs/snippets/qs-intro-full.md`  
> Upstream SHA: `bd716a1e31df4ecec1c2efd3c06c225c899739ef`

**Original:** Strapi takes advantage of the ability of the `qs` library to parse nested objects to create more complex queries.

**中文译文:** Strapi 利用 `qs` library 解析 nested objects 的能力来构建更复杂的 query。

**Original:** Use `qs` directly to generate complex queries instead of creating them manually. Examples in this documentation showcase how you can use `qs`.

**中文译文:** 建议直接使用 `qs` 生成复杂 query，而不是手工拼接 URL。文档中的 JavaScript 示例会展示 `qs` 的具体用法。

**Original:** You can also use the interactive query builder if you prefer the online tool.

**中文译文:** 如果不希望在本机使用 `qs`，也可以使用在线 [interactive query builder](/cms/api/rest/interactive-query-builder)。
